#ifndef _MAIN_H_
#define _MAIN_H_

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>

int _printf(const char *format, ...);
char *convert(unsigned int num, int base);
int _putchar(char c);

#endif /* _MAIN_H_ i*/
